# Copia questo file in ".env" (stessa cartella) e incolla la tua chiave.
# Serve solo se la ricerca libri mostra "Ricerca libri non disponibile al
# momento" per limiti di quota dell'API pubblica di Google Books.
# Come ottenerla: vedi il README, sezione "Variabili d'ambiente opzionali".
GOOGLE_BOOKS_API_KEY=
